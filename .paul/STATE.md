# Project State

## Project Reference

See: .paul/PROJECT.md (updated 2026-08-15)

**Core value:** Boutique PH resorts get one system that prevents overbookings, syncs rates/availability across major OTAs in real time, and collects local downpayments — without stitching together separate channel-manager, front-desk, and payment tools.

**Current focus:** Phase 3 (Hotel admin config UI) — in progress. 03-01 (backend CRUD) complete. 03-02 (frontend admin UI) plan drafted, awaiting audit/apply.

## Current Position

Milestone: v0.1 Initial Release
Phase: 3 of 7 (Hotel admin config UI) — Complete (2/2 plans). Ready to plan Phase 4 or Phase 5 (both depend only on Phase 1).
Plan: None active
Status: Phase transition complete — PROJECT.md evolved (also caught up Phase 2's evolution, which was missed at the time), ROADMAP.md marked, ready for next /paul:plan
Last activity: 2026-08-18 — Phase 3 transition complete

Progress:
- Milestone: [████░░░░░░] ~29% (2 of 7 phases complete)
- Phase 3: [██████████] 100% (2 of 2 plans complete)

## Loop Position

Current loop state:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [03-02 complete — Phase 3 done, transitioning]
```

**Scope-split note:** ROADMAP.md originally estimated Phase 2 at ~4 plans total, with the remaining work as one item ("React calendar grid UI + walk-in booking form"). Since no frontend exists yet in this project, that single item was split into 3 plans on subsystem/vertical-slice boundaries (PAUL's own sizing guidance: 2-3 tasks/plan, split on subsystem lines) — 02-04 (scaffold + login, this plan), 02-05 (calendar grid UI), 02-06 (booking form UI). Phase 2 is now 6 plans total, not 4. Decided during planning, not asked as a checkpoint — routine PAUL process judgment per standing instruction, reported here for visibility.

## Accumulated Context

### Decisions

| Decision | Phase | Impact |
|----------|-------|--------|
| Xendit-only payments (GCash/QR Ph/cards), no separate Maya integration | Pre-build | All payment work targets one provider |
| Central Channex account held by founder; hotels never see Channex directly | Pre-build | ChannelMapping/webhook design assumes single Channex credential set |
| Rate plans modeled as their own entity, distinct from RoomType | Pre-build | ARI has both a room-type dimension (availability) and a rate-plan dimension (price/minStay) — see the corrected DailyInventory/RatePlanDailyRate split below |
| Testing-scenario-first policy: acceptance criteria include explicit edge-case scenarios before build starts | Pre-build | Applies to every future PLAN, not optional per-phase |
| Security pass (security-review + gsd-security-auditor) required specifically on payment, webhook, and auth/multi-tenant phases | Pre-build | See .paul/SPECIAL-FLOWS.md |
| gsd-verifier goal-backward audit reserved for one pre-launch gate (before first real hotel goes live), not run per-phase | Pre-build | Avoids per-phase ceremony while product is still being shaped |
| No parallel subagent spawning by default; flag expensive operations before running them | Pre-build | See .paul/config.md Preferences + Usage Governance |
| 2026-08-15: Enterprise audit performed on 01-01-PLAN.md. Applied 5 must-have, 4 strongly-recommended upgrades. Deferred 2. Verdict: conditionally acceptable (amended) | Phase 1 | Plan strengthened: cuid ids, onDelete:Restrict, soft-delete columns, RatePlan hotelId denormalized, idempotent smoke test |
| 2026-08-15: Corrected DailyInventory design before 01-02 build — availableCount shared per [roomTypeId, date], not per rate plan. Added separate RatePlanDailyRate for price/minStay per [ratePlanId, date] | Phase 1 (caught during 01-02 planning) | Prevents same physical rooms being oversold across multiple rate plans of one room type; PROJECT.md decision log updated to match |
| 2026-08-15: Enterprise audit performed on 01-02-PLAN.md. Applied 1 must-have, 4 strongly-recommended upgrades. Deferred 1. Verdict: conditionally acceptable (amended) | Phase 1 | Added DB-level CHECK constraint enforcing bookedCount+heldCount<=availableCount — the actual overbooking backstop, not just documented intent; per-rate-plan verification instead of aggregate; zero-Rooms edge case covered |
| 2026-08-15: Enterprise audit performed on 01-03-PLAN.md. Applied 4 must-have, 2 strongly-recommended upgrades. Deferred 3. Verdict: conditionally acceptable (amended) | Phase 1 | Fixed a real financial-correctness bug (BookingItem's flat per-night price snapshot contradicted 01-02's per-date rate model — renamed to totalPriceSnapshot, full-stay total); added accountability fields (Booking.createdByUserId, Payment.processedByUserId) and Booking.totalAmount for financial reconciliation |
| 2026-08-15: Phase 1 (Data model + inventory foundation) complete — all 12 models live, migrated, and proven against real data across 3 plans | Phase 1 → Phase 2 transition | PROJECT.md evolved (Validated section populated); ROADMAP.md marked Phase 1 complete; git commit created for the phase |
| 2026-08-15: Auth strategy decided — JWT (stateless), not server-side sessions | Phase 2 (checkpoint:decision, user-confirmed) | Simpler, no Session table; accepted tradeoff: no instant token revocation before 12h expiry — documented as a deliberate risk, not a gap |
| 2026-08-15: Enterprise audit performed on 02-01-PLAN.md. Applied 5 must-have, 3 strongly-recommended upgrades. Deferred 3. Verdict: conditionally acceptable (amended) | Phase 2 | Added rate limiting, JWT_SECRET strength check, error-handling discipline, password policy on create-user.ts, and explicit documentation of the JWT-revocation tradeoff |
| 2026-08-15: 02-01 APPLY complete. security-review found zero HIGH/MEDIUM vulns. gsd-security-auditor found a real timing side-channel — malformed dummy bcrypt hash (57 chars, not 60) short-circuited comparison (~0.2ms vs ~260ms real), letting an attacker distinguish "no such email" from "wrong password" via response timing despite identical bodies. Fixed with a genuine cost-12 hash constant; independently re-verified, 10/10 mitigations closed | Phase 2 | Caught before shipping — neither my own manual review nor the automated smoke test (body-equality only) caught the timing gap; only the dedicated security-auditor's live timing measurement did |
| 2026-08-16: Enterprise audit performed on 02-02-PLAN.md. Applied 2 must-have, 2 strongly-recommended upgrades. Deferred 0 new. Verdict: conditionally acceptable (amended) | Phase 2 | Fixed an error-handling scope gap (try/catch didn't cover the tenant-ownership check) and a data-completeness gap (unseeded dates would silently vanish from the calendar response instead of being marked seeded:false) |
| 2026-08-16: 02-02 APPLY complete. security-review found zero HIGH/MEDIUM vulns. gsd-security-auditor ran live adversarial probes (hotelId spoofing, forced DB errors, unseeded-date edge case) and confirmed 8/8 mitigations hold — no new gaps found | Phase 2 | First real query-level multi-tenant isolation in the project, proven under live attack simulation, not just code review |
| 2026-08-18: 02-03 locking strategy decided (user-confirmed) — conditional atomic UPDATE per date inside one Prisma $transaction, not explicit SELECT FOR UPDATE | Phase 2 (checkpoint:decision, resolved during PLAN authoring rather than deferred to APPLY) | Relies on Postgres's native row-level locking during UPDATE; avoids SELECT FOR UPDATE's lock-ordering deadlock risk; DB CHECK constraint remains as backstop either way. Logged in Carl (PAUL domain) alongside this table. |
| 2026-08-18: 02-03-PLAN.md created with a new "Assumptions Requiring Review" section (4 flagged assumptions: booking status=CONFIRMED not PENDING_PAYMENT, no Payment record created yet, exact-email-only guest matching, quantity-aware availability check) — plan-scoped only, not yet formalized as a standing config.md convention | Phase 2 | User must review this section before UNIFY closes; new paul-plan-critic subagent (~/.claude/agents/paul-plan-critic.md) exists and can be spawned against this plan before APPLY if desired, not yet run |
| 2026-08-18: Enterprise audit performed on 02-03-PLAN.md. Applied 5 must-have, 3 strongly-recommended upgrades. Deferred 3. Verdict: conditionally acceptable (amended) | Phase 2 | Closed a real SQL-injection-shaped gap (unspecified raw-SQL construction) and a real missing-authorization-boundary gap (any authenticated role, including HOUSEKEEPING, could create bookings) — both would fail a real security/compliance review if shipped unamended. Also closed a silent-undercharge risk (missing RatePlanDailyRate rows), added Decimal-safe money arithmetic, a max-stay cap, and a lightweight duplicate-submission guard. AC-1..6 amended to AC-1..9. |
| 2026-08-18: User instructed — follow the PAUL loop autonomously for routine routing/process decisions (which track, audit-vs-apply-vs-critic ordering); only stop for genuinely critical/irreversible calls | Process | Applies going forward for this project; routing menus replaced with "decide and report," reserving stops for hard-halt-tier decisions like the locking-strategy choice already made this session |
| 2026-08-18: 02-03 APPLY complete. security-review found 1 real HIGH finding (guest.email used unvalidated in Prisma `where` filters — filter-operator injection, e.g. `{"not": null}`, could match an arbitrary existing Guest instead of the intended exact-match lookup) — fixed same session with a type check + dedicated regression test. gsd-security-auditor independently re-probed the fix live with 9 attack-shape variants, proved the HOUSEKEEPING role gate genuinely blocks all DB access (not just the response), found and closed the one untested byte-identical-404 case (ratePlan belongs to wrong roomType), and confirmed 14/14 mitigations hold | Phase 2 | Second plan in this project (after 02-01's timing side-channel) where the security pass caught something the enterprise audit and manual review both missed — the two-layer review is catching genuinely different problem classes, not duplicating |
| 2026-08-18: 02-03 UNIFY complete — core overbooking-prevention guarantee proven live (50 concurrent requests for 1 room → exactly 1 succeeds), not just designed | Phase 2 | Conditional-UPDATE transaction pattern now proven and reusable by Phase 4's Channex booking_new webhook handler |
| 2026-08-18: Phase 2's remaining scope ("React calendar grid UI + walk-in booking form") split into 3 plans (02-04 scaffold+login, 02-05 calendar grid, 02-06 booking form) since no frontend existed yet — PAUL's own vertical-slice sizing guidance applied, decided during planning without a checkpoint | Phase 2 | Phase 2 now estimated at 6 plans total, not 4 |
| 2026-08-18: Enterprise audit performed on 02-04-PLAN.md. Applied 2 must-have, 4 strongly-recommended upgrades. Deferred 2. Verdict: conditionally acceptable (amended) | Phase 2 | Caught a real structural gap (a checkpoint assumed Login.tsx/Dashboard.tsx were built by a task that didn't exist) and a real email-enumeration-regression risk (an unspecified frontend error message could silently reopen the exact vulnerability 02-01's security audit closed at the backend layer) — third plan in this project where review caught a security guarantee not surviving consumption by a later layer |
| 2026-08-18: 02-04 APPLY complete, checkpoint approved by user live in a real browser (all 6 manual checks: login landed, wrong-credentials declined, correct-credentials logged in, refresh persisted session, corrupted-token forced re-login, logout worked). 02-04 UNIFY complete | Phase 2 | First frontend in the project, first plan in this project verified by a human actually clicking through it rather than only automated checks |
| 2026-08-18: User declined a dedicated design-taste pass on the frontend for now ("designing is for later") — proceeding with plain functional Tailwind styling | Phase 2 | Applies to 02-05/02-06 too, until a dedicated design pass is explicitly requested |
| 2026-08-18: Enterprise audit performed on 02-05-PLAN.md. Applied 3 must-have, 2 strongly-recommended upgrades. Deferred 2. Verdict: conditionally acceptable (amended) | Phase 2 | Caught a real timezone bug (browser-local-timezone "today" computation would silently misalign with the backend's Manila-anchored date semantics), a classic stale-response race condition (rapid Next/Prev clicking could leave the grid showing the wrong week with no error), and a missing-rate-cell crash/misleading-blank-cell case |
| 2026-08-18: 02-05 APPLY complete, checkpoint approved by user against a deliberately-seeded test hotel exercising both audit-flagged edge cases (sold-out date, missing-rate date) live, not just the happy path. 02-05 UNIFY complete | Phase 2 | First real database-backed data view in the project (not just auth); Phase 2 now 5 of 6 plans complete, only the booking form (02-06) remains |
| 2026-08-18: Enterprise audit performed on 02-06-PLAN.md. Applied 1 must-have, 2 strongly-recommended upgrades. Deferred 2. Verdict: conditionally acceptable (amended) | Phase 2 | Caught a real high-likelihood bug (booking form's default check-in date could silently be a past date if staff had navigated the grid backward, failing the single most common submit-the-default interaction every time) — smallest finding count of the three frontend audits, plan already applied lessons from 02-04/02-05 |
| 2026-08-18: security-review on 02-06 found zero HIGH/MEDIUM findings — cleanest security pass of the project, consistent with this plan being a pure consumer of 02-03's already-fully-audited endpoint | Phase 2 | No new attack surface introduced by the booking form |
| 2026-08-18: 02-06 APPLY complete, checkpoint approved by user live in a real browser across two sessions of testing — confirmed valid booking + live grid update, sold-out 409, invalid-range 400, missing-field validation, the audit-fixed past-date-default bug (verified fixed after navigating the grid backward), HOUSEKEEPING-role form-hiding, and (after a live network-unreachable diagnostic that revealed a dev-only Vite-proxy artifact, documented not fixed) in-flight selector/nav disabling under Slow 4G throttling. 02-06 UNIFY complete | Phase 2 | Every audit finding across all 6 plans this phase was independently re-confirmed live by the user, not just claimed fixed |
| 2026-08-18: **Phase 2 (Front-desk booking core) complete — 6/6 plans.** Full front-desk loop (login → see real availability → create a walk-in booking → grid updates live) working end-to-end, user-verified, security-audited (one real HIGH finding in 02-03 found and fixed same-session; zero findings elsewhere) | Phase 2 → Phase 3/4 transition | ROADMAP.md updated to ✅ Complete. Next: plan Phase 3 (Hotel admin config UI) or Phase 4 (Channex integration) — both depend only on Phase 1 per ROADMAP.md, either can go next |
| 2026-08-18: User chose Phase 3 (Hotel admin config UI) over Phase 4. 03-01-PLAN.md created (RoomType CRUD, RatePlan CRUD, hotel policy settings) with an Assumptions Requiring Review section flagging the Room-unit-CRUD-not-in-scope gap | Phase 3 | Backend-only plan, no frontend; 03-02 (admin UI) to follow |
| 2026-08-18: Enterprise audit performed on 03-01-PLAN.md (ran after explicit user approval, per config.md's Usage Governance cost-gate — audit passes aren't auto-run without confirmation even under the standing "decide autonomously" instruction). Applied 1 must-have, 3 strongly-recommended upgrades. Deferred 3. Verdict: conditionally acceptable (amended) | Phase 3 | Closed a real data-integrity gap: nothing blocked deleting a room type/rate plan with future active bookings, which would've silently orphaned a guest's reservation from staff view. Also added accountability fields (lastModifiedByUserId/deletedByUserId on RoomType/RatePlan, mirroring Booking/Payment's Phase-1 precedent), tightened try/catch scope (repeat of a bug class 02-02 already caught once), and empty-PATCH-body rejection. Deferred: basePrice sanity ceiling, duplicate-submission guard, rate limiting — all lower-stakes/self-correctable or explicitly out of security-review's scope |
| 2026-08-18: 03-01 APPLY complete — RoomType/RatePlan CRUD + hotel policy settings built, all 9 ACs (including the audit-added deletion-safety and accountability-trail ACs) live-verified via a throwaway script against the real dev DB (21/21 checks passed), existing 02-03 booking-flow smoke test re-confirmed passing unmodified. security-review found zero HIGH/MEDIUM findings — no mass-assignment surface, consistent tenant-scoped lookup pattern reused from roomTypes.ts/bookings.ts. 03-01 UNIFY complete | Phase 3 | First hotel-configuration data in the project created through the API rather than a hand-run script; Phase 3 now 1 plan complete, frontend admin UI (03-02) not yet planned |
| 2026-08-18: 03-02-PLAN.md created — frontend Admin.tsx (room type/rate plan CRUD, policy settings) consuming 03-01's API | Phase 3 | Last plan of Phase 3 (estimated); depends_on 03-01 (genuine — imports its API shapes) |
| 2026-08-18: Enterprise audit performed on 03-02-PLAN.md (ran after explicit user approval, same cost-gate as 03-01). Applied 0 must-have, 4 strongly-recommended upgrades. Deferred 1. Verdict: conditionally acceptable (amended) | Phase 3 | No release-blocking finding this time (pure UI consumer of an already-audited API) — but caught a real UX-correctness risk: naive refetch-after-mutation could silently clobber an admin's unsaved in-progress edit on an unrelated form elsewhere on the same page. Also caught a basePrice string-vs-number typing risk (Prisma Decimal serializes to a string, easy to mistype as number and get silent wrong behavior), missing pre-fill requirement on edit forms, and a checkpoint step (verifying 03-01's biggest fix through the new UI) that was marked optional when it shouldn't have been |
| 2026-08-18: 03-02 APPLY complete — Admin.tsx (room type/rate plan CRUD, policy settings) built, checkpoint approved by user live in browser (create/edit room type, create/edit rate plan, policy save all confirmed with a screenshot). security-review (scoped to this plan's actual diff, not re-reviewing 03-01's already-audited CRUD) found zero HIGH/MEDIUM findings. 03-02 UNIFY complete. **Phase 3 (Hotel admin config UI) complete — 2/2 plans** | Phase 3 → transition | AC-9 and AC-7 verified via blanket "all working" approval + code review confirming correct implementation, not individually re-screenshotted this round — noted in 03-02-SUMMARY.md, not treated as a blocker. Dev-environment hiccup during checkpoint resume: unrelated app ("GiftPilot") was squatting on port 3000, blocking our backend from starting; identified and stopped with user's explicit approval before restarting |
| 2026-08-18: **Major business-model pivot** — from direct-to-hotel B2B SaaS to an agency/reseller model. Agency signs a representation agreement with each hotel, sets OTA listing prices above the hotel's base rate, keeps the spread. Confirmed flow: OTA collects guest payment, deducts its commission, remits the net to the **agency's** bank account (not the hotel's); agency separately remits each hotel's base-rate share. Two new milestones queued: v0.2 (agency payout/reconciliation ledger) and v0.3 (direct-booking site + FB integration) | Process/business | PROJECT.md and ROADMAP.md updated (What This Is, Core Value, Business Context, Key Decisions, Target Users, Compliance Constraints, new Future Milestones section). v0.1's existing Phase 1-7 technical roadmap is unaffected in shape — still "finish the core booking engine to a demo" per founder's explicit instruction; only Phase 5 (PayMongo) got a scope-narrowing note flagged for its own planning time. This reverses the earlier "founder never holds guest money" PayMongo-merchant-of-record decision (marked Superseded, not deleted) — real compliance/tax question (BSP money-service registration? revenue recognition treatment?) flagged as needing a lawyer/accountant opinion before scaling past a pilot hotel, not yet obtained |

### Deferred Issues

| Issue | Origin | Effort | Revisit |
|-------|--------|--------|---------|
| Written service agreement (scope, liability, support definition) for hotel clients — terms now locked (2-week free pilot, 12-month founding-partner discount, no lock-in on hotel's side), template drafting + lawyer review not yet done | Pre-build discussion, refined 2026-08-18 | S | Before first paying hotel goes live |
| DTI (optional, only if using a trade name) + BIR Form 1901 registration (required) — sole proprietorship, founder as sole owner; wife is a collaborator with no formal ownership stake, informal arrangement given marital community-property status, confirm property regime with an accountant/lawyer at filing time | Business plumbing discussion, 2026-08-18 | S | Before first paying hotel invoice, not before building |
| Outside developer review of Phase 4/5 (Channex + PayMongo concurrency/webhook correctness) — skipping for now, founder networking informally for a lead in the meantime, no urgency yet | Technical review discussion, 2026-08-18 | S | Before pilot hotel converts to paying, or before onboarding a second hotel — whichever comes first. See paul-024 in Carl for full scope/budget/checklist. |
| Cross-hotel "super admin" view for founder (monitoring sync health/bookings across all hotels) | Pre-build discussion | M | Decide before or during roadmap planning |
| Whether pilot hotels are already lined up or this is purely pre-sales | Pre-build discussion | - | Affects whether early phases build against real or assumed hotel data |
| Whether this Supabase dev project becomes the Phase 7 staging environment or stays a separate throwaway dev DB | 01-01 audit | S | Phase 7 (pre-launch gate) |
| Keeping DailyInventory.availableCount in sync when Rooms are added/removed/marked OOS after initial 365-day seeding | 01-02 audit | M | Phase 3 (room management UI) and Phase 6 (housekeeping OOS status) |
| Backend has no CORS middleware — fine for local dev (frontend uses a Vite dev-proxy, same-origin from the browser's view), but will block every API call once frontend (Vercel) and backend (Railway/Render) are on different real origins | 02-04 audit | S | Before or during whichever plan first handles real (non-dev-proxy) deployment |
| No physical Room (unit/label) CRUD anywhere in the product — new room types created via 03-01's admin API seed with availableCount:0 and stay that way until Rooms exist via the manual dev script | 03-01 plan/audit | M | Not currently scheduled in any named phase; revisit if hotel admins need to be fully self-sufficient without founder involvement |
| Legal/tax opinion needed on the agency holding guest-derived OTA payouts before remitting hotel shares — BSP money-service-business registration question (BSP Circular 1198 "merchant payment acceptance" language is the specific clause to check against), revenue-recognition treatment for tax. Research (2026-08-18) found no clean precedent either way — leans "probably ordinary commission-agent, not MSB" but not certain. Founder's read: workable in practice, other PH companies operate this way informally; still wants a lawyer to confirm before scaling past a pilot hotel — not a blocker for the demo build | Business-model pivot discussion, 2026-08-18; researched + risk-accepted-pending-lawyer 2026-08-18 | M | Before onboarding a real paying hotel under the agency model, not before building the payout ledger (v0.2) — good audit-trail software helps this conversation, doesn't replace it |
| Signed hotel representation/management agreement (authorizes agency to list/manage OTA presence) — separate from the general service agreement already flagged | Business-model pivot discussion, 2026-08-18 | S | Same timing as the general service agreement — before first paying hotel goes live |
| RatePlan schema doesn't yet distinguish hotel's base rate from marked-up OTA listing price (single `basePrice` field) — needs resolving before Phase 4's Channex ARI push or v0.2's payout ledger, whichever is planned first | Business-model pivot discussion, 2026-08-18 | M | Phase 4 (Channex integration) or v0.2 (payout ledger) planning, whichever comes first |
| Rate-parity risk for v0.3's owned marketplace app — app may sell a room at a different price than the same room's Agoda/Booking.com listing (likely during the 2-month no-commission promo period), which can breach OTA rate-parity clauses requiring the price not be undercut elsewhere. Must confirm each OTA's current clause or keep prices matched across channels | v0.3 redefinition discussion, 2026-08-18 | S | Before v0.3's marketplace app goes live publicly, not before it's planned |

### Blockers/Concerns

None yet.

### Git State

Last commit: 68660bd — feat(02-front-desk-booking-core): calendar grid query API
Branch: master
Remote: https://github.com/jbt-gif/-TA-Channel-Management (pushed 2026-08-16)
Feature branches merged: none
Note: graphify's own auto-regenerated files (graphify-out/*) and its own tooling dirs (.agents/, .claude/, skills-lock.json) were left unstaged/untracked in this commit — not part of the PAUL build, left for the user or graphify's own process to commit separately.

## Session Continuity

Last session: 2026-08-18
Stopped at: Phase 3 complete (03-01 + 03-02 both done). Transition to next phase in progress (evolving PROJECT.md, marking ROADMAP.md, git commit).
Next action: Complete phase transition, then choose Phase 4 (Channex integration) or Phase 5 (PayMongo) — both depend only on Phase 1 per ROADMAP.md. Still open, not blocking: the Room-unit-CRUD gap; 02-03's Assumptions 1-2; CORS backend config needed before real deployment; the business-pivot deferred items (legal/tax opinion, representation agreement, RatePlan base-rate-vs-OTA-price schema question, v0.3 rate-parity mitigation).
Resume file: .paul/phases/03-hotel-admin-config-ui/03-02-SUMMARY.md

---
*STATE.md — Updated after every significant action*
